# Durable (recommended for tick capture)
appendonly yes
appendfsync everysec

# Snapshot (optional extra safety)
save 60 10000
dir /data

# Optional caps (uncomment if needed)
# maxmemory 4gb
# maxmemory-policy noeviction
